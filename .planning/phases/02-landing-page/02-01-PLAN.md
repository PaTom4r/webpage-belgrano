---
phase: 02-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/content/site.ts
  - src/lib/content/verticales.ts
  - src/components/animations/scroll-reveal.tsx
  - src/components/animations/animated-counter.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - LAND-03
  - LAND-04
  - LAND-06
  - LAND-07
  - LAND-08
  - ANIM-01
  - ANIM-02
  - ANIM-05

must_haves:
  truths:
    - "Content data for all 4 verticals is importable from lib/content/verticales.ts"
    - "Nav links and footer copy are importable from lib/content/site.ts"
    - "ScrollReveal wraps any Server Component child and fades+slides it in on scroll"
    - "AnimatedCounter counts from 0 to target with GSAP when scrolled into view"
    - "prefers-reduced-motion disables all Framer Motion animations globally (MotionConfig in layout)"
  artifacts:
    - path: "src/lib/content/verticales.ts"
      provides: "Vertical data type + 4 vertical objects"
      exports: ["Vertical", "verticales"]
    - path: "src/lib/content/site.ts"
      provides: "Nav links, footer data, global copy strings"
      exports: ["navLinks", "footerData", "siteConfig"]
    - path: "src/components/animations/scroll-reveal.tsx"
      provides: "Framer Motion whileInView wrapper"
      exports: ["ScrollReveal"]
    - path: "src/components/animations/animated-counter.tsx"
      provides: "GSAP counter with ScrollTrigger"
      exports: ["AnimatedCounter"]
  key_links:
    - from: "src/components/animations/scroll-reveal.tsx"
      to: "motion/react"
      via: "import { motion } from 'motion/react'"
      pattern: "from 'motion/react'"
    - from: "src/components/animations/animated-counter.tsx"
      to: "src/lib/gsap-config.ts"
      via: "import { gsap, ScrollTrigger, useGSAP } from '@/lib/gsap-config'"
      pattern: "from '@/lib/gsap-config'"
    - from: "src/app/layout.tsx"
      to: "motion/react MotionConfig"
      via: "MotionConfig reducedMotion='user' wraps children"
      pattern: "MotionConfig"
---

<objective>
Install Phase 2 dependencies and create the foundational building blocks every subsequent plan depends on: content data files, animation primitives, and global reduced-motion config.

Purpose: Plans 02-04 (sections) import from these files. If they don't exist first, sections have nothing to reference. Wave 1 means these run before any section work starts.
Output: lib/content/ data files, animations/ primitives, updated layout.tsx with MotionConfig.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-page/02-CONTEXT.md
@.planning/phases/02-landing-page/02-RESEARCH.md

@src/app/globals.css
@src/lib/gsap-config.ts
@src/app/layout.tsx
</context>

<interfaces>
<!-- Phase 1 contracts the executor needs. -->

From src/lib/gsap-config.ts:
```typescript
import gsap from 'gsap'
import { ScrollTrigger } from 'gsap/ScrollTrigger'
import { useGSAP } from '@gsap/react'
gsap.registerPlugin(ScrollTrigger, useGSAP)
export { gsap, ScrollTrigger, useGSAP }
```

From src/app/globals.css @theme tokens:
```css
--color-bg: #ffffff
--color-bg-section: #f9fafb
--color-dark: #09090b
--color-text: #111827
--color-text-secondary: #6b7280
--color-accent: #000000
--color-border: #e5e7eb
--font-sans: var(--font-geist), 'Inter', system-ui, sans-serif
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create content data files</name>
  <files>
    src/lib/content/site.ts
    src/lib/content/verticales.ts
  </files>
  <read_first>
    - src/app/page.tsx (understand current smoke test — will be replaced in Plan 05)
    - src/app/layout.tsx (understand current layout before Plan 01 modifies it)
  </read_first>
  <action>
**Step 1 — Install Phase 2 deps:**
```bash
cd /c/Users/patri/Documents/Code/trabajo/webpage-belgrano
npm install react-hook-form@^7.72.0 zod@^4.3.6 @hookform/resolvers@^5.2.2
```
Verify install succeeds (no peer dependency warnings that would break build).

**Step 2 — Create src/lib/content/verticales.ts:**

```typescript
// src/lib/content/verticales.ts
// Content for the 4 Belgrano service verticals.
// Used by: VerticalesSection (landing), vertical detail pages (Phase 3).

export interface Vertical {
  slug: string
  name: string
  tagline: string
  description: string
  icon: string // Lucide icon name (used as string label — actual icon rendered by section component)
  metrics?: string[]
  clients?: string[]
}

export const verticales: Vertical[] = [
  {
    slug: 'bots',
    name: 'Bots Conversacionales',
    tagline: 'Tu negocio responde solo. Siempre.',
    description:
      'Chatbots con IA que atienden, califican y derivan leads en WhatsApp, web y redes sociales — 24/7, sin costo operacional adicional. Automatizamos la primera línea de atención para que tu equipo se enfoque en cerrar, no en contestar.',
    icon: 'MessageCircle',
    metrics: ['70–85% resolución automática', '30–50% reducción de costos de atención'],
    clients: ['Clínica Las Condes', 'Seguros CLC'],
  },
  {
    slug: 'dooh',
    name: 'DOOH & Señalética Digital',
    tagline: 'Tu marca en movimiento, donde importa.',
    description:
      'Diseñamos, producimos y operamos redes de pantallas digitales en puntos de alto tráfico — malls, clínicas, corporativos y espacios públicos. Contenido dinámico, actualizable en tiempo real, con métricas de impacto.',
    icon: 'Monitor',
    metrics: ['500+ pantallas gestionadas', '+40% recordación de marca vs. señalética estática'],
    clients: ['TNT Sports', 'Warner Bros.', 'AFP Modelo'],
  },
  {
    slug: 'producciones',
    name: 'Producciones Digitales',
    tagline: 'Contenido que convierte, no solo impresiona.',
    description:
      'Videos, motion graphics, fotografía y assets digitales producidos con IA y talento humano. Desde reels de producto hasta campañas integradas. Velocidad de agencia boutique, calidad de producción profesional.',
    icon: 'Film',
    metrics: ['3x velocidad vs. producción tradicional', 'Costos 40% menores'],
  },
  {
    slug: 'academy',
    name: 'Belgrano Academy',
    tagline: 'Tu equipo con superpoderes de IA.',
    description:
      'Programas de formación y talleres prácticos para equipos de marketing, ventas y operaciones que quieren incorporar IA de verdad — no solo hablar de ella. Desde fundamentos hasta implementación de herramientas.',
    icon: 'GraduationCap',
    metrics: ['200+ profesionales formados', '98% satisfacción'],
    clients: ['Clínica Las Condes', 'AFP Modelo'],
  },
]
```

**Step 3 — Create src/lib/content/site.ts:**

```typescript
// src/lib/content/site.ts
// Global site strings: nav links, footer copy, company metadata.
// Used by: Navbar, Footer, metadata in page.tsx.

export const siteConfig = {
  name: 'Belgrano',
  tagline: 'IA, Marketing & Estrategia',
  description:
    'Agencia chilena de IA aplicada, marketing digital y producción de contenido para empresas que quieren crecer en serio.',
  url: 'https://belgrano.cl',
  email: 'hola@belgrano.cl',
  whatsapp: '+56912345678', // placeholder — update before Phase 5
}

export interface NavLink {
  label: string
  href: string
}

export const navLinks: NavLink[] = [
  { label: 'Qué hacemos', href: '#what-we-do' },
  { label: 'Cómo trabajamos', href: '#how-it-works' },
  { label: 'Resultados', href: '#stats' },
  { label: 'Contacto', href: '#cta' },
]

export const footerData = {
  tagline: 'Hacemos que la IA trabaje para tu negocio.',
  links: [
    { label: 'Bots Conversacionales', href: '/verticales/bots' },
    { label: 'DOOH & Señalética', href: '/verticales/dooh' },
    { label: 'Producciones Digitales', href: '/verticales/producciones' },
    { label: 'Belgrano Academy', href: '/verticales/academy' },
    { label: 'Nosotros', href: '/nosotros' },
    { label: 'Contacto', href: '#cta' },
  ],
  copyright: `© ${new Date().getFullYear()} Grupo Belgrano SpA. Todos los derechos reservados.`,
}
```
  </action>
  <verify>
    <automated>cd /c/Users/patri/Documents/Code/trabajo/webpage-belgrano && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - npm install exits 0 with react-hook-form, zod, @hookform/resolvers in node_modules
    - src/lib/content/verticales.ts exports Vertical interface + verticales array with 4 items (slugs: bots, dooh, producciones, academy)
    - src/lib/content/site.ts exports siteConfig, navLinks (4 items), footerData
    - TypeScript compiles without errors on these files
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ScrollReveal and AnimatedCounter animation primitives</name>
  <files>
    src/components/animations/scroll-reveal.tsx
    src/components/animations/animated-counter.tsx
  </files>
  <read_first>
    - src/lib/gsap-config.ts (required import for AnimatedCounter)
  </read_first>
  <action>
**Create src/components/animations/scroll-reveal.tsx:**

```typescript
// src/components/animations/scroll-reveal.tsx
// Framer Motion scroll-triggered entrance animation wrapper.
// Use this for ALL section content below the fold.
// For above-fold (Hero), use initial/animate directly — NOT this component.
'use client'

import { motion } from 'motion/react'

interface ScrollRevealProps {
  children: React.ReactNode
  delay?: number
  className?: string
  /** Override y-offset (default 30px). Set to 0 for fade-only on mobile. */
  y?: number
}

export function ScrollReveal({ children, delay = 0, className, y = 30 }: ScrollRevealProps) {
  return (
    <motion.div
      initial={{ opacity: 0, y }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: '-50px' }}
      transition={{ duration: 0.5, delay, ease: [0.25, 0.1, 0.25, 1] }}
      className={className}
    >
      {children}
    </motion.div>
  )
}
```

**Create src/components/animations/animated-counter.tsx:**

```typescript
// src/components/animations/animated-counter.tsx
// GSAP-powered counter that animates from 0 to target when scrolled into view.
// Respects prefers-reduced-motion: skips animation, shows final value immediately.
// Import GSAP exclusively from '@/lib/gsap-config' — never directly from 'gsap'.
'use client'

import { useRef } from 'react'
import { gsap, ScrollTrigger, useGSAP } from '@/lib/gsap-config'

interface AnimatedCounterProps {
  target: number
  prefix?: string
  suffix?: string
  duration?: number
  className?: string
}

export function AnimatedCounter({
  target,
  prefix = '',
  suffix = '',
  duration = 2,
  className,
}: AnimatedCounterProps) {
  const counterRef = useRef<HTMLSpanElement>(null)

  useGSAP(() => {
    const mm = gsap.matchMedia()

    // Full animation for users with no motion preference
    mm.add('(prefers-reduced-motion: no-preference)', () => {
      const obj = { val: 0 }
      gsap.to(obj, {
        val: target,
        duration,
        ease: 'power2.out',
        scrollTrigger: {
          trigger: counterRef.current,
          start: 'top 80%',
          once: true,
        },
        onUpdate() {
          if (counterRef.current) {
            counterRef.current.textContent =
              prefix + Math.round(obj.val).toLocaleString('es-CL') + suffix
          }
        },
      })
    })

    // Reduced motion: show final value immediately, no animation
    mm.add('(prefers-reduced-motion: reduce)', () => {
      if (counterRef.current) {
        counterRef.current.textContent = prefix + target.toLocaleString('es-CL') + suffix
      }
    })
  }, { scope: counterRef })

  return (
    <span ref={counterRef} className={className}>
      {prefix}0{suffix}
    </span>
  )
}
```
  </action>
  <verify>
    <automated>cd /c/Users/patri/Documents/Code/trabajo/webpage-belgrano && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - scroll-reveal.tsx: "use client", imports from "motion/react", exports ScrollReveal with initial/whileInView/viewport props
    - animated-counter.tsx: "use client", imports from "@/lib/gsap-config" (not gsap directly), uses useGSAP, handles prefers-reduced-motion via gsap.matchMedia()
    - Both files typecheck cleanly
  </done>
</task>

<task type="auto">
  <name>Task 3: Add MotionConfig to root layout for global reduced-motion support</name>
  <files>
    src/app/layout.tsx
  </files>
  <read_first>
    - src/app/layout.tsx (MUST read current content before modifying)
  </read_first>
  <action>
Update src/app/layout.tsx to add `MotionConfig reducedMotion="user"` around children. This is the single global reduced-motion gate for all Framer Motion animations in the app.

The layout currently looks like:
```typescript
import type { Metadata } from 'next'
import { Geist, Inter } from 'next/font/google'
import { Analytics } from '@vercel/analytics/react'
import './globals.css'
// ... fonts, metadata ...
export default function RootLayout({ children }) {
  return (
    <html lang="es" className={...}>
      <body style={{ borderTopStyle: 'solid' }}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
```

New layout adds MotionConfig import and wrapper. The GSAP hydration fix (`borderTopStyle: 'solid'`) MUST remain — do not remove it.

IMPORTANT: MotionConfig is a client boundary — add `'use client'` only to the wrapper, not the layout file itself (layout.tsx must stay a Server Component for metadata export). Create a thin `ClientProviders` component:

**Create src/components/client-providers.tsx:**
```typescript
// src/components/client-providers.tsx
// Thin client boundary for providers that need "use client".
// layout.tsx imports this to keep the root layout as a Server Component.
'use client'

import { MotionConfig } from 'motion/react'

export function ClientProviders({ children }: { children: React.ReactNode }) {
  return (
    <MotionConfig reducedMotion="user">
      {children}
    </MotionConfig>
  )
}
```

**Update src/app/layout.tsx** to import and use ClientProviders:
```typescript
import type { Metadata } from 'next'
import { Geist, Inter } from 'next/font/google'
import { Analytics } from '@vercel/analytics/react'
import { ClientProviders } from '@/components/client-providers'
import './globals.css'

const geistSans = Geist({
  subsets: ['latin'],
  variable: '--font-geist',
})

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
})

export const metadata: Metadata = {
  metadataBase: new URL('https://belgrano.cl'),
  title: {
    template: '%s | Belgrano',
    default: 'Belgrano — IA, Marketing & Estrategia',
  },
  description: 'Agencia chilena de IA aplicada, marketing digital y estrategia de negocio.',
  openGraph: {
    siteName: 'Belgrano',
    locale: 'es_CL',
    type: 'website',
  },
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="es" className={`${geistSans.variable} ${inter.variable}`}>
      {/* GSAP hydration fix: borderTopStyle pre-matches what ScrollTrigger injects — do not remove */}
      <body style={{ borderTopStyle: 'solid' }}>
        <ClientProviders>
          {children}
        </ClientProviders>
        <Analytics />
      </body>
    </html>
  )
}
```

Note: Analytics stays outside ClientProviders — it is its own client component and does not need MotionConfig context.
  </action>
  <verify>
    <automated>cd /c/Users/patri/Documents/Code/trabajo/webpage-belgrano && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - src/components/client-providers.tsx exists with "use client", imports MotionConfig from "motion/react", exports ClientProviders
    - src/app/layout.tsx imports ClientProviders and wraps children with it
    - GSAP hydration fix (borderTopStyle: 'solid') still present on body element
    - TypeScript compiles without errors
    - grep "MotionConfig" src/app/layout.tsx returns match (via ClientProviders import)
    - grep "reducedMotion" src/components/client-providers.tsx returns "reducedMotion="user""
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

```bash
cd /c/Users/patri/Documents/Code/trabajo/webpage-belgrano

# 1. Deps installed
node -e "require('react-hook-form'); require('zod'); require('@hookform/resolvers/zod'); console.log('deps OK')"

# 2. Content files exist with correct exports
npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"

# 3. Required files exist
ls src/lib/content/site.ts src/lib/content/verticales.ts src/components/animations/scroll-reveal.tsx src/components/animations/animated-counter.tsx src/components/client-providers.tsx

# 4. Key patterns present
grep -r "reducedMotion" src/components/client-providers.tsx
grep -r "from '@/lib/gsap-config'" src/components/animations/animated-counter.tsx
grep -r "from 'motion/react'" src/components/animations/scroll-reveal.tsx
grep -r "whileInView" src/components/animations/scroll-reveal.tsx
```
</verification>

<success_criteria>
- react-hook-form@^7.72.0, zod@^4.3.6, @hookform/resolvers@^5.2.2 installed
- 5 new files created: site.ts, verticales.ts, scroll-reveal.tsx, animated-counter.tsx, client-providers.tsx
- layout.tsx updated with ClientProviders wrapper (MotionConfig reducedMotion="user")
- GSAP hydration fix still in place (body borderTopStyle: 'solid')
- npx tsc --noEmit passes with 0 errors
- All GSAP code in animated-counter.tsx imports from @/lib/gsap-config exclusively
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-01-SUMMARY.md`
</output>
