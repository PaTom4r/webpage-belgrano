---
phase: 04-about-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/content/about.ts
  - src/components/sections/about-narrative-section.tsx
  - src/components/sections/about-values-section.tsx
  - src/components/sections/about-team-section.tsx
  - src/app/nosotros/page.tsx
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03

must_haves:
  truths:
    - "Visiting /nosotros loads a complete page without errors"
    - "The company narrative section tells Belgrano's story (who, what, where, since when)"
    - "The values section communicates the unique integrated-services differentiator (bots + screens + content + training)"
    - "The team positioning section explains how the team works without requiring individual photos"
    - "All three sections are visible on mobile without truncation"
    - "ScrollReveal animations fire on scroll for all sections below the fold"
  artifacts:
    - path: "src/lib/content/about.ts"
      provides: "All About page copy as typed TypeScript constants"
      exports: ["aboutContent"]
    - path: "src/components/sections/about-narrative-section.tsx"
      provides: "ABOUT-01 — company history and mission section"
    - path: "src/components/sections/about-values-section.tsx"
      provides: "ABOUT-02 — values/differentiators section with icon cards"
    - path: "src/components/sections/about-team-section.tsx"
      provides: "ABOUT-03 — team positioning section (no photos)"
    - path: "src/app/nosotros/page.tsx"
      provides: "The /nosotros route composing all three sections"
  key_links:
    - from: "src/app/nosotros/page.tsx"
      to: "src/lib/content/about.ts"
      via: "named import aboutContent"
      pattern: "import.*aboutContent.*from.*about"
    - from: "src/app/nosotros/page.tsx"
      to: "src/components/sections/about-*.tsx"
      via: "React component composition in JSX"
      pattern: "AboutNarrativeSection|AboutValuesSection|AboutTeamSection"
    - from: "src/components/sections/about-*.tsx"
      to: "src/components/animations/scroll-reveal.tsx"
      via: "ScrollReveal wrapper on section content"
      pattern: "ScrollReveal"
---

<objective>
Build the /nosotros About page: company narrative, values/differentiators, and team positioning — all sourced from a typed content file and composed with existing layout primitives.

Purpose: Give potential B2B clients who want to know who they are hiring a clear picture of Belgrano's story, differentiated offering, and working model.
Output: 5 files — content data file, 3 section components, 1 page route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and patterns extracted from existing codebase. Executor uses these directly. -->

From src/components/layout/section.tsx:
```tsx
export function Section({
  children,
  className?: string,
  dark?: boolean,   // dark=true: bg-dark text-bg | false: bg-bg text-text (default)
  id?: string,
}: { children: React.ReactNode; className?: string; dark?: boolean; id?: string })
```

From src/components/layout/container.tsx:
```tsx
export function Container({ children, className?: string }: { children: React.ReactNode; className?: string })
// max-w-7xl, px-4 sm:px-6 lg:px-8, mx-auto
```

From src/components/animations/scroll-reveal.tsx:
```tsx
'use client'
export function ScrollReveal({ children, delay?: number, className?: string, y?: number }: ScrollRevealProps)
// import from 'framer-motion' (NOT 'motion/react')
// Use whileInView — correct for all sections BELOW the fold
```

From src/app/verticales/[slug]/page.tsx — breadcrumb pattern:
```tsx
<div className="bg-bg border-b border-border">
  <Container>
    <Breadcrumb verticalName={vertical.name} />
  </Container>
</div>
```
// Note: existing Breadcrumb takes verticalName prop. For About, build inline breadcrumb
// in page.tsx to avoid coupling — no need for a generic component.

Design tokens (from globals.css @theme):
- bg: #ffffff, dark: #09090B, text: #111827, text-secondary: #6B7280
- border: border-border (var), bg-section: #f9fafb
- Font: font-geist (headlines), font-inter (body)
- Headline weights: 800-900, tight tracking (-tracking-tight or -tracking-tighter)
- Dark sections: bg-dark text-bg
- Animations: import from 'framer-motion', NOT 'motion/react'

Brand context (from CLAUDE.md + CONTEXT.md):
- Grupo Belgrano: AI-native Chilean marketing and business strategy agency
- Tagline: "Tu negocio. Nuestra inteligencia."
- Located in Santiago, Chile
- Tone: Directo, seguro, cálido. No corporativo ni frío.
- Key differentiator: Only agency that builds the bot, installs the screens, produces the content, AND trains the team
- Verticals: Bots Conversacionales, DOOH & Señalética, Producciones Digitales, Belgrano Academy
- Clients: CLC, Seguros CLC, TNT Sports/Warner Bros
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content data file + three About section components</name>
  <files>
    src/lib/content/about.ts
    src/components/sections/about-narrative-section.tsx
    src/components/sections/about-values-section.tsx
    src/components/sections/about-team-section.tsx
  </files>

  <read_first>
    - src/lib/content/site.ts (siteConfig reference — brand voice)
    - src/components/layout/section.tsx (Section component API)
    - src/components/animations/scroll-reveal.tsx (ScrollReveal API)
    - src/components/sections/vertical-metrics-section.tsx (pattern for dark-bg grid sections)
  </read_first>

  <action>
**Step 1: Create src/lib/content/about.ts**

Export a single `aboutContent` constant typed as a plain object with three top-level keys: `narrative`, `values`, `team`.

Content to write (use this exact copy, adjust tone to brand voice):

```
narrative: {
  headline: "Quiénes somos",
  subheadline: "Una agencia chilena construida sobre IA real, no promesas.",
  paragraphs: [
    "Grupo Belgrano nació en Santiago con una convicción clara: la inteligencia artificial no es una moda — es la herramienta más poderosa que una empresa puede poner a trabajar hoy. Desde el primer día, construimos todo en torno a esa idea.",
    "Somos una agencia integrada de marketing, IA y estrategia de negocios. Esto significa que no derivamos a terceros: el mismo equipo que diseña la estrategia, la ejecuta. Sin teléfono descompuesto, sin pérdida de contexto.",
    "Nuestros clientes incluyen aseguradoras, canales de televisión y empresas de servicios que eligieron crecer de forma inteligente. Hoy los acompañamos en automatización de conversaciones, presencia digital en pantallas, producción de contenido y formación de equipos."
  ],
}

values: {
  headline: "Por qué Belgrano",
  subheadline: "Cuatro verticales. Un solo equipo. Ningún intermediario.",
  items: [
    {
      icon: "bot",
      title: "IA aplicada desde el día uno",
      description: "No usamos IA como buzz word. Construimos bots reales, flujos de automatización y sistemas que tu equipo puede operar desde mañana."
    },
    {
      icon: "layers",
      title: "Servicios integrados",
      description: "Bots + pantallas + contenido + formación. Somos la única agencia que cubre todos estos frentes sin subcontratar ni fragmentar la estrategia."
    },
    {
      icon: "map-pin",
      title: "100% chilena",
      description: "Entendemos el mercado local, los canales locales y cómo hablan tus clientes. No adaptamos plantillas de afuera — creamos estrategias desde adentro."
    },
    {
      icon: "users",
      title: "Tu equipo, no solo tu agencia",
      description: "Formamos a tu gente a través de Belgrano Academy. Cuando terminamos, tu equipo sabe usar las herramientas que instalamos. El conocimiento se queda."
    }
  ]
}

team: {
  headline: "Cómo trabajamos",
  subheadline: "Un equipo pequeño, capaz de moverse rápido.",
  paragraphs: [
    "Belgrano opera con un modelo de equipo integrado: estrategas, productores, desarrolladores y especialistas en IA trabajando en el mismo proyecto, sin silos. Cada vertical tiene su líder técnico, y todos reportan a un solo punto de contacto.",
    "No tercerizamos la parte crítica. Las herramientas que te recomendamos son las que usamos todos los días. Si algo no funciona, somos los primeros en saberlo — y los primeros en arreglarlo.",
    "Trabajamos con contratos por proyecto o retainerías mensuales. El proceso empieza siempre igual: diagnóstico honesto, propuesta con números reales, ejecución medible."
  ],
  highlights: [
    { label: "Diagnóstico", description: "Entendemos tu negocio antes de proponer nada." },
    { label: "Estrategia", description: "Plan claro con métricas definidas desde el inicio." },
    { label: "Ejecución", description: "Implementación sin intermediarios ni sorpresas." },
    { label: "Formación", description: "Tu equipo aprende a operar lo que construimos." }
  ]
}
```

Use TypeScript interfaces for each section. Export the typed `aboutContent` constant.

**Step 2: Create src/components/sections/about-narrative-section.tsx**

Server Component (no 'use client'). Layout:
- Section component with `dark={false}` (light bg)
- Inside: headline as h1 (text-4xl sm:text-5xl font-black tracking-tighter), subheadline as p (text-xl text-text-secondary mt-4)
- Below: 3 paragraphs from `narrative.paragraphs`, each wrapped in `<ScrollReveal delay={i * 0.1}>`, styled as `text-lg leading-relaxed text-text-secondary`
- Max width prose container: `mx-auto max-w-3xl` for paragraphs
- Heading centered, paragraphs left-aligned

Import: `Section` from layout, `ScrollReveal` from animations, `aboutContent` from lib/content/about.

**Step 3: Create src/components/sections/about-values-section.tsx**

Server Component. Layout:
- Section component with `dark={true}` (dark bg for contrast — same pattern as Stats/CTA sections)
- Headline + subheadline centered (text-bg color since dark section)
- Below: 4-card grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mt-12`
- Each card: `rounded-xl border border-white/10 bg-white/5 p-6`
- Card content: icon (inline SVG, 24×24, stroke white/70), title (text-base font-bold text-bg mt-4), description (text-sm text-bg/70 mt-2 leading-relaxed)
- Each card wrapped in `<ScrollReveal delay={i * 0.1}>`

Icons (inline SVG, stroke-based, 24×24, no fill, currentColor):
- "bot": a simple robot/chat-bot icon — use a square with two dots (eyes) and a mouth line
- "layers": three stacked horizontal rectangles
- "map-pin": pin/location icon
- "users": two overlapping person silhouettes

Keep icons minimal — 2-3 path elements max. Use `stroke="currentColor" strokeWidth="1.5" fill="none"`.

**Step 4: Create src/components/sections/about-team-section.tsx**

Server Component. Layout:
- Section component with `dark={false}` (light bg, alternate from values)
- Section background class override: add `className="bg-bg-section"` on Section to use the #f9fafb alternate
- Headline + subheadline (text-4xl sm:text-5xl font-black tracking-tighter, centered)
- Two paragraphs from `team.paragraphs[0]` and `team.paragraphs[1]`, each in ScrollReveal, `max-w-3xl mx-auto text-lg leading-relaxed text-text-secondary`
- Below paragraphs: 4-step horizontal process strip — `grid grid-cols-2 lg:grid-cols-4 gap-4 mt-12`
  - Each highlight: a card with `rounded-xl border border-border bg-bg p-6`
  - Number badge: `w-8 h-8 rounded-full bg-dark text-bg flex items-center justify-center text-sm font-bold` (numbers 01–04)
  - Highlight label: `text-base font-bold text-text mt-3`
  - Highlight description: `text-sm text-text-secondary mt-1`
- Third paragraph from `team.paragraphs[2]` below the grid, centered, `max-w-2xl mx-auto text-base text-text-secondary mt-10`, wrapped in ScrollReveal

Each card/paragraph in ScrollReveal with stagger delay.
  </action>

  <verify>
    <automated>cd "C:/Users/patri/Documents/Code/trabajo/webpage-belgrano" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - src/lib/content/about.ts exists and exports `aboutContent` with narrative, values, team keys
    - grep -r "aboutContent" src/lib/content/about.ts returns the export line
    - src/components/sections/about-narrative-section.tsx exists and contains "AboutNarrativeSection"
    - src/components/sections/about-values-section.tsx exists and contains "AboutValuesSection"
    - src/components/sections/about-team-section.tsx exists and contains "AboutTeamSection"
    - grep -r "ScrollReveal" src/components/sections/about-narrative-section.tsx returns matches
    - grep -r "import.*framer-motion" src/components/sections/about-*.tsx returns zero results (ScrollReveal handles the import)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>

  <done>Four files created with no TypeScript errors. All three section components import from aboutContent and use ScrollReveal for below-fold animations.</done>
</task>

<task type="auto">
  <name>Task 2: Compose /nosotros page route</name>
  <files>
    src/app/nosotros/page.tsx
  </files>

  <read_first>
    - src/app/verticales/[slug]/page.tsx (reference for page composition pattern)
    - src/components/layout/container.tsx (Container API)
    - src/app/layout.tsx (metadata.title template — use "%s | Belgrano" pattern)
  </read_first>

  <action>
Create `src/app/nosotros/page.tsx` as a Server Component.

**Metadata export:**
```tsx
export const metadata: Metadata = {
  title: 'Nosotros',
  description: 'Conoce a Grupo Belgrano: agencia chilena de IA aplicada, marketing digital y estrategia de negocio. Un equipo integrado que construye, instala y forma.',
}
```
The title template in layout.tsx adds "| Belgrano" automatically → renders as "Nosotros | Belgrano".

**Page structure:**
```tsx
<main>
  {/* Breadcrumb bar — same pattern as vertical pages */}
  <div className="bg-bg border-b border-border">
    <Container>
      <nav aria-label="Breadcrumb" className="py-4">
        <ol className="flex items-center gap-2 text-sm text-text-secondary">
          <li><Link href="/" className="transition-opacity hover:opacity-60">Home</Link></li>
          <li aria-hidden="true" className="text-border">/</li>
          <li className="font-medium text-text" aria-current="page">Nosotros</li>
        </ol>
      </nav>
    </Container>
  </div>

  <AboutNarrativeSection />   {/* ABOUT-01: company story, dark=false */}
  <AboutValuesSection />       {/* ABOUT-02: values grid, dark=true */}
  <AboutTeamSection />         {/* ABOUT-03: how we work, bg-section */}
  <CtaSection />               {/* Reuse existing CTA — consistent conversion point */}
</main>
```

Imports needed:
```tsx
import type { Metadata } from 'next'
import Link from 'next/link'
import { Container } from '@/components/layout/container'
import { AboutNarrativeSection } from '@/components/sections/about-narrative-section'
import { AboutValuesSection } from '@/components/sections/about-values-section'
import { AboutTeamSection } from '@/components/sections/about-team-section'
import { CtaSection } from '@/components/sections/cta-section'
```

No `generateStaticParams` needed — this is a static route, not dynamic.
  </action>

  <verify>
    <automated>cd "C:/Users/patri/Documents/Code/trabajo/webpage-belgrano" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - src/app/nosotros/page.tsx exists
    - grep "AboutNarrativeSection\|AboutValuesSection\|AboutTeamSection\|CtaSection" src/app/nosotros/page.tsx returns 4 matches (import + usage)
    - grep "export const metadata" src/app/nosotros/page.tsx returns a match
    - grep "aria-current=\"page\"" src/app/nosotros/page.tsx returns a match (breadcrumb)
    - npx tsc --noEmit exits 0
    - npm run build exits 0 (confirms /nosotros route is statically generated without errors)
  </acceptance_criteria>

  <done>/nosotros route is complete. Build succeeds. The page composes all three about sections plus the shared CtaSection.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` — zero errors
2. `npm run build` — build succeeds, /nosotros appears in the static route list
3. `npm run dev` — visit http://localhost:3000/nosotros and confirm:
   - Page loads without console errors
   - Breadcrumb shows "Home / Nosotros"
   - Narrative section has headline, subheadline, 3 paragraphs
   - Values section (dark bg) has 4 icon cards with distinct icons
   - Team section (light alternate bg) has paragraphs + 4-step process strip (01–04)
   - CTA section at bottom (reused from landing)
   - All sections visible on mobile viewport (no horizontal overflow)
</verification>

<success_criteria>
- /nosotros loads at localhost:3000/nosotros with zero console errors
- ABOUT-01: Narrative section presents company history, mission, and client context
- ABOUT-02: Values section (dark bg) shows 4 differentiator cards with inline SVG icons
- ABOUT-03: Team section shows how Belgrano works + 4-step process strip (Diagnóstico → Estrategia → Ejecución → Formación)
- All content readable on mobile without truncation
- Build passes: `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-about-page/04-01-SUMMARY.md` following the summary template at @$HOME/.claude/get-shit-done/templates/summary.md
</output>
